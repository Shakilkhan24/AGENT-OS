node_modules/
dist/
/release/
test-results/
playwright-report/
__pycache__/
*.log
cmd.sh
.env
.env.*
!.env.example
.minimal-*-test-*/

# M9.5 — pilot harness output. Operators point --out here; the harness
# never auto-invokes, so the directory is opt-in only.
/pilot-out/
pilot-out/

# M9.2 — integrity manifest emitted at the `retained` release phase.
/release/**/MANIFEST.sha256
/release/**/*.sha256
